# Firebase Function Image Resizer

This function will trigger when an image is uploaded at `/tmp/` folder in Firebase Storage.

It will create multiple version of Images 
{XLarge, Large, Medium, Small, Blur}